<div class="container">
  <h4>Editais</h4>

  <% if @editais != nil %>
    <table class="table table-condensed">
      <thead>
        <tr>
          <th>Número</th>
          <th>Tipo</th>
          <th>Área(s)</th>
          <th>Publicação</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
      <% @editais.each do |edital| %>
        <tr>
          <td><%= edital.numero %></td>
          <td><%= edital.tipo == 'concurso' ? 'Concurso Público' : 'Processo Seletivo Simplificado' %></td>
          <td>
            <%= edital.areas.to_sentence if edital.areas %>
          </td>
          <td></td>
          <td>
            <%= link_to edit_edital_path(edital), class: "btn btn-primary" do %>
              <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span> Editar
            <% end %>
            <%= link_to edital_word_path(edital), class: "btn btn-primary" do %>
              <span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span> Word
            <% end %>
            <%= link_to edital_word_path(edital), class: "btn btn-primary" do %>
              <span class="glyphicon glyphicon-file" aria-hidden="true"></span> Word
            <% end %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  <% else %>
    Nada!
  <% end %>
  <%= link_to new_edital_path, class: "btn btn-success" do %>
    <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> Novo Edital
  <% end %>
</div>